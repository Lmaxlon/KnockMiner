/*package com.badlogic.drop;

import com.badlogic.gdx.scenes.scene2d.InputEvent;
import com.badlogic.gdx.scenes.scene2d.utils.ClickListener;

public class MyClickListener extends ClickListener {
    private EmptyWindow emptyWindow;

    public MyClickListener(EmptyWindow emptyWindow) {
        this.emptyWindow = emptyWindow;
    }

    @Override
    public void clicked(InputEvent event, float x, float y) {
        emptyWindow.buyDialog.setVisible(false);
        emptyWindow.buyDialog.hide();
        emptyWindow.isWindowOpen = false; // Установка флага в false при закрытии окна
    }
}
*/